Run `python admin-app/manage.py runserver` to get the development server running.
If it is the first time (or you rebuild the containers) do not forget to run the migrations `python manage.py migrate` first and setup a superuser via `python manage.py createsuperuser`
To debug, use the `launch.json` inside `.vscode`.